export const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
export function toast(message){const t=document.querySelector('#toast');t.textContent=message;t.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.classList.remove('show'),2200)}
export function fmtDate(iso){if(!iso)return 'Not scheduled';const d=new Date(iso);if(Number.isNaN(d.getTime()))return 'Not scheduled';return d.toLocaleString([], {day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}
export function dueLabel(iso){if(!iso)return '';const d=new Date(iso), now=new Date();if(Number.isNaN(d.getTime()))return '';const same=d.toDateString()===now.toDateString();if(d<now)return 'OVERDUE';if(same)return 'TODAY';return 'UPCOMING'}
export function leadCard(x){const due=dueLabel(x.followup_at);return `<article class="lead-card" data-open="${esc(x.id)}"><div class="lead-top"><div><h3>${esc(x.name)}</h3><p>${esc(x.wing)} • ${esc(x.city||'—')}</p></div><span class="status ${esc(x.status.toLowerCase())}">${esc(x.status)}</span></div><p class="req">${esc(x.requirement)}</p><div class="lead-meta"><span>${due?`<b>${due}</b> • `:''}${esc(fmtDate(x.followup_at))}</span></div><div class="quick-actions"><button data-call="${esc(x.mobile)}" aria-label="Call ${esc(x.name)}">☎ Call</button><button data-wa="${esc(x.mobile)}" aria-label="WhatsApp ${esc(x.name)}">WhatsApp</button><button data-open="${esc(x.id)}">Open ›</button></div></article>`}
